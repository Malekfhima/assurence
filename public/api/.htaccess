RewriteEngine On

# Redirige toutes les requêtes (sauf fichiers/dossiers existants) vers index.php
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.php?route=$1 [QSA,L]
